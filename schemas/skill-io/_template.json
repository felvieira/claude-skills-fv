{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Skill I/O — Base Template",
  "description": "Base schema that every skill I/O schema should follow. Copy and extend.",
  "type": "object",
  "required": ["input", "output"],
  "properties": {
    "input": {
      "type": "object",
      "description": "Subagent input fields. Add required[] and properties here."
    },
    "output": {
      "type": "object",
      "required": ["confidence"],
      "properties": {
        "confidence": {
          "type": "string",
          "enum": ["high", "medium", "low"],
          "description": "Confidence level of this subagent's result"
        }
      },
      "description": "Subagent output fields. Always include confidence."
    }
  }
}
