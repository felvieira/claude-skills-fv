# Bundled Plugin Catalog

These manifests group existing skills into task-oriented compositions. They are references only: skills stay in `skills/` and the complete kit remains installable as before.

Use `node scripts/route-task.mjs "<task>"` to inspect a recommendation. See `policies/plugin-catalog.md` for the contract and safety rules.
