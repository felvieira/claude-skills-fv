{
  "schema_version": 2,
  "scope": "Leitura estática de quatorze arquivos: dez arquivos de código do núcleo MCP, proteção de ferramentas, entrypoint e schema de template, mais quatro manifestos/configurações auxiliares. Regras operacionais do kit são separadas de exemplos de aplicação; não há afirmação de produto ou RPA implantado.",
  "reviewed_files": [
    {
      "path": "src/middleware.ts",
      "sha256": "44dee90de93282939a1fdfae5ccae635637c98b321c98c4e610653f6490fb396"
    },
    {
      "path": "src/hooks/useAuth.ts",
      "sha256": "94116a0e5fc7df63b49c93f7bdafcd54742cfd57e8368ffcab54ca30034fc161"
    },
    {
      "path": "src/hooks/usePagination.ts",
      "sha256": "b7d64454a5c4ef11dceddd037311a9ed3eb9672dfd304fde1af444f9b890c56c"
    },
    {
      "path": "mcp-server/src/lib/pipeline-engine.ts",
      "sha256": "cbf2be0b0d025e1019fc76f9973c5a0c7a33a8b5132cc81ac31837a29bfb506b"
    },
    {
      "path": "mcp-server/src/lib/cross-call-dedup.ts",
      "sha256": "3e1045be91a9e40bf22c802c321312eec702c2dda99fb2f939b3e3269d741ac0"
    },
    {
      "path": "mcp-server/src/services/file-reader.ts",
      "sha256": "1244013c81dece135da72402a67aa6f4b4091985e809a6d4afe5ca172bdac847"
    },
    {
      "path": "mcp-server/src/services/playwright.ts",
      "sha256": "a6bd36465f79e004f38dae5f6d2779fcaafd5f685161971664fe2cfe631af195"
    },
    {
      "path": "hooks/scripts/permission-ladder-guard.mjs",
      "sha256": "05833af14ea3512139202c0da051a6134c55b0e49f73cf5d009ed16ab4b21b9d"
    },
    {
      "path": "mcp-server/src/index.ts",
      "sha256": "519646766040e3c21d84bf45e9c494f0f0cd045fdbd4fc564da69c7e6b2f0322"
    },
    {
      "path": "mcp-server/package.json",
            "sha256": "f6813cffb396c1ace24454b850dbb3b0dd8d55cd8b95637ef6c114b0675744c4"
    },
    {
      "path": "mcp-server/tsconfig.json",
      "sha256": "fee86899bb77179611ed16b73572159d567e76a4717522e567900dbb17ac0804"
    },
    {
      "path": "templates/stack-default/apps/web/package.json",
      "sha256": "892a0474772bc6b8129751fb269c166413e6b9ec43d7d32dcca8bbe79d00a0f6"
    },
    {
      "path": "bench/ab/package.json",
      "sha256": "a83aba737b3069c359a5e5f52ec0d735ad9efe4cd0675d794a961fc8255bd34f"
    },
    {
      "path": "templates/stack-default/apps/web/src/db/schema.ts",
      "sha256": "5579cbdbd8726780e8f4086c29b35899b51605bbe3a137d7d031082fdd63b11b"
    }
  ],
  "overview": {
    "summary": "Este repositório é o Dev Team Kit: um kit de skills, comandos e um servidor MCP para apoiar agentes de coding em tarefas de desenvolvimento.",
    "purpose": "Ele organiza especialistas e ferramentas reutilizáveis para descoberta, implementação, testes, segurança, documentação, inteligência de sessão e integração com serviços auxiliares. O pacote principal executável identificado no código é o servidor MCP em mcp-server/.",
    "audience": "Agentes compatíveis com MCP e equipes que usam o kit para conduzir tarefas de software com governança e etapas especializadas.",
    "technologies": [
      {"name":"Node.js","version":">=18","role":"Runtime do pacote MCP e execução do entrypoint compilado.","evidence":[{"path":"mcp-server/package.json","quote":"    \"node\": \">=18\""}]},
      {"name":"TypeScript","version":"^5.7.0","role":"Linguagem e compilador do servidor MCP.","evidence":[{"path":"mcp-server/package.json","quote":"    \"typescript\": \"^5.7.0\""},{"path":"mcp-server/package.json","quote":"    \"build\": \"tsc\""}]},
      {"name":"Model Context Protocol SDK","version":"^1.12.1","role":"Cria o servidor MCP e expõe ferramentas ao agente.","evidence":[{"path":"mcp-server/package.json","quote":"    \"@modelcontextprotocol/sdk\": \"^1.12.1\""},{"path":"mcp-server/src/index.ts","quote":"import { McpServer } from \"@modelcontextprotocol/sdk/server/mcp.js\";"}]},
      {"name":"Zod","version":"^3.24.0","role":"Biblioteca declarativa de schemas/validação usada pelo servidor.","evidence":[{"path":"mcp-server/package.json","quote":"    \"zod\": \"^3.24.0\""},{"path":"mcp-server/src/index.ts","quote":"import { z } from \"zod\";"}]},
      {"name":"ES Modules + Node16","version":"ES2022","role":"Configuração de módulo, resolução e alvo de compilação TypeScript.","evidence":[{"path":"mcp-server/package.json","quote":"  \"type\": \"module\","},{"path":"mcp-server/tsconfig.json","quote":"    \"target\": \"ES2022\","},{"path":"mcp-server/tsconfig.json","quote":"    \"module\": \"Node16\","}]},
      {"name":"Next.js + React + Tailwind","version":"Next 15.3.2 / React 19 / Tailwind 4.1.7","role":"Stack do template web stack-default; é template de aplicação, não o runtime principal do MCP.","evidence":[{"path":"templates/stack-default/apps/web/package.json","quote":"    \"next\": \"15.3.2\","},{"path":"templates/stack-default/apps/web/package.json","quote":"    \"react\": \"^19.0.0\","},{"path":"templates/stack-default/apps/web/package.json","quote":"    \"tailwindcss\": \"^4.1.7\""}]},
      {"name":"Playwright","version":"1.44","role":"Dependência do benchmark A/B em bench/ab; não foi tratado como executor do servidor MCP.","evidence":[{"path":"bench/ab/package.json","quote":"    \"playwright\": \"1.44\""}]}
    ],
    "entrypoints": [
      {"name":"dev-team-kit-mcp","role":"Binário publicado que aponta para dist/index.js.","evidence":[{"path":"mcp-server/package.json","quote":"    \"dev-team-kit-mcp\": \"dist/index.js\""}]},
      {"name":"mcp-server/src/index.ts","role":"Inicializa o McpServer, registra as ferramentas e conecta transporte stdio.","evidence":[{"path":"mcp-server/src/index.ts","quote":"const server = new McpServer({"},{"path":"mcp-server/src/index.ts","quote":"  const transport = new StdioServerTransport();\n  await server.connect(transport);"}]},
      {"name":"skills/ e .claude/commands/","role":"Superfícies do kit consumidas como skills e comandos do agente; a ligação completa não foi revisada nesta amostra.","evidence":[{"path":"mcp-server/package.json","quote":"  \"description\": \"MCP server for the Dev Team Kit - 38 tools backed by 73 skills + 45 slash commands"}]}
    ],
    "commands": [
      {"command":"npm run build","purpose":"Compila TypeScript para dist.","evidence":[{"path":"mcp-server/package.json","quote":"    \"build\": \"tsc\""}]},
      {"command":"npm start","purpose":"Inicia o entrypoint compilado do servidor MCP.","evidence":[{"path":"mcp-server/package.json","quote":"    \"start\": \"node dist/index.js\""}]},
      {"command":"npm test","purpose":"Executa os testes compilados de deduplicação, manifesto e roteador de plugins.","evidence":[{"path":"mcp-server/package.json","quote":"    \"test\": \"node dist/lib/cross-call-dedup.test.js && node dist/lib/skill-manifest.test.js && node dist/lib/plugin-router.test.js\""}]}
    ],
    "structure": [
      {"path":"mcp-server/src/index.ts","role":"Bootstrap do servidor MCP e registro das ferramentas.","evidence":[{"path":"mcp-server/src/index.ts","quote":"const server = new McpServer({"},{"path":"mcp-server/src/index.ts","quote":"  const transport = new StdioServerTransport();"}]},
      {"path":"mcp-server/src/services/","role":"Serviços auxiliares, incluindo leitura de arquivos, busca, scraping, geração de imagem e instruções de browser.","evidence":[{"path":"mcp-server/src/index.ts","quote":"import * as fileReader from \"./services/file-reader.js\";"},{"path":"mcp-server/src/index.ts","quote":"import { searchBrave, searchCompetitors, searchDesignReferences } from \"./services/brave-search.js\";"},{"path":"mcp-server/src/index.ts","quote":"import { generateImage } from \"./services/fal-ai.js\";"},{"path":"mcp-server/src/index.ts","quote":"import { buildScreenshotInstruction, buildImageExtractionInstruction, buildScrapeInstruction } from \"./services/playwright.js\";"}]},
      {"path":"mcp-server/src/lib/","role":"Bibliotecas internas de classificação, pipelines, contexto, compressão, eventos e roteamento.","evidence":[{"path":"mcp-server/src/index.ts","quote":"import { classifyTask } from \"./lib/classifier.js\";"},{"path":"mcp-server/src/index.ts","quote":"import { buildPipeline, getNextStep } from \"./lib/pipeline-engine.js\";"}]},
      {"path":"templates/stack-default/apps/web/","role":"Template separado para iniciar aplicações web com Next.js, React e Tailwind.","evidence":[{"path":"templates/stack-default/apps/web/package.json","quote":"  \"name\": \"web\","}]}
    ],
    "gaps": [
      "O propósito geral foi sintetizado a partir do manifesto e dos pontos de entrada revisados; a matriz completa de 73 skills não foi relida nesta execução.",
      "Não foi executado npm install, npm run build ou npm test do mcp-server nesta rodada; os comandos acima são declarados pelo package.json.",
      "Versões do template e do benchmark são componentes auxiliares e não devem ser confundidas com a stack do servidor MCP."
    ]
  },
  "architecture": {
    "summary": "O código revisado se divide em limites de aplicação (middleware e hooks), serviços do núcleo MCP, controles de execução e integrações externas. O mapa abaixo mostra somente relações sustentadas pelos trechos revisados.",
    "contexts": [
      {
        "id": "kit-runtime",
        "label": "Runtime do kit e servidor MCP",
        "summary": "Núcleo executável que registra ferramentas MCP, carrega serviços e aplica políticas do kit.",
        "evidence": [
          {
            "path": "mcp-server/src/index.ts",
            "quote": "const server = new McpServer({\n  name: \"dev-team-kit-mcp-server\",\n  version: \"1.0.0\",\n});"
          }
        ]
      },
      {
        "id": "consumer-template",
        "label": "Aplicação consumidora de referência",
        "summary": "Template web separado do runtime MCP; serve como exemplo de aplicação que o kit pode apoiar.",
        "evidence": [
          {
            "path": "templates/stack-default/apps/web/package.json",
            "quote": "    \"dev\": \"next dev --turbopack\",\n    \"build\": \"next build\",\n    \"start\": \"next start\","
          }
        ]
      },
      {
        "id": "benchmark",
        "label": "Benchmark e experimento",
        "summary": "Área de benchmark independente, sem evidência de ser parte do caminho de produção do servidor.",
        "evidence": [
          {
            "path": "bench/ab/package.json",
            "quote": "  \"name\": \"ab\",\n  \"version\": \"1.0.0\",\n  \"main\": \"index.js\","
          }
        ]
      }
    ],
    "levels": [
      {
        "id": "context",
        "label": "Contexto",
        "summary": "Kit de skills com servidor MCP, templates de aplicação e benchmarks em contextos distintos.",
        "evidence": [
          {
            "path": "mcp-server/package.json",
            "quote": "  \"description\": \"MCP server for the Dev Team Kit - 38 tools backed by 73 skills + 45 slash commands (incl. /constitution, /analyze, /checklist for spec-driven development, /canary for gradual rollout, /diff-impact for ripple analysis, /instinct-export|import|promote, /multi-plan, /aside, /skill-health, /pattern-conformity), routing, context packs, working sets, image generation, scraping and session intelligence\","
          }
        ]
      },
      {
        "id": "container",
        "label": "Containers e limites",
        "summary": "Servidor MCP, hooks/policies, aplicação web de referência e benchmark possuem responsabilidades e ciclos próprios.",
        "evidence": [
          {
            "path": "mcp-server/package.json",
            "quote": "  \"main\": \"dist/index.js\",\n  \"bin\": {\n    \"dev-team-kit-mcp\": \"dist/index.js\"\n  },"
          }
        ]
      },
      {
        "id": "component",
        "label": "Componentes",
        "summary": "Serviços e bibliotecas internas implementam classificação, pipelines, leitura, deduplicação e guards.",
        "evidence": [
          {
            "path": "mcp-server/src/index.ts",
            "quote": "import { buildPipeline, getNextStep } from \"./lib/pipeline-engine.js\";"
          }
        ]
      }
    ],
    "nodes": [
      {
        "id": "auth-middleware",
        "label": "Next middleware",
        "kind": "control",
        "responsibility": "Aplica fronteiras de rota, sessão por cookie e headers de segurança.",
        "evidence": [
          {
            "path": "src/middleware.ts",
            "quote": "  // Rota protegida sem sessão: redireciona pra login\n  if (!hasSession) {\n    const loginUrl = new URL('/login', request.url);\n    loginUrl.searchParams.set('redirect', pathname);\n    return NextResponse.redirect(loginUrl);\n  }"
          }
        ]
      },
      {
        "id": "auth-hook",
        "label": "useAuth",
        "kind": "module",
        "responsibility": "Coordena login, cadastro, logout, cache de queries e navegação do cliente.",
        "evidence": [
          {
            "path": "src/hooks/useAuth.ts",
            "quote": "  const loginMutation = useMutation({\n    mutationFn: async (input: LoginInput) => {\n      const { data } = await api.post<ApiResponse<AuthResponse>>('/auth/login', input);\n      return data.data;\n    },"
          }
        ]
      },
      {
        "id": "auth-api",
        "label": "API de autenticação",
        "kind": "external",
        "responsibility": "Endpoint consumido pelo hook para iniciar a sessão.",
        "evidence": [
          {
            "path": "src/hooks/useAuth.ts",
            "quote": "const { data } = await api.post<ApiResponse<AuthResponse>>('/auth/login', input);"
          }
        ]
      },
      {
        "id": "pagination-hook",
        "label": "usePagination",
        "kind": "module",
        "responsibility": "Mantém página, parâmetros e limites de navegação sobre uma query paginada.",
        "evidence": [
          {
            "path": "src/hooks/usePagination.ts",
            "quote": "  const query = usePaginatedQuery<T>(queryKey, url, allParams);"
          }
        ]
      },
      {
        "id": "paginated-query",
        "label": "usePaginatedQuery",
        "kind": "interface",
        "responsibility": "Interface de consulta reutilizada pelo hook de paginação.",
        "evidence": [
          {
            "path": "src/hooks/usePagination.ts",
            "quote": "import { usePaginatedQuery } from './useApi';"
          }
        ]
      },
      {
        "id": "pipeline-engine",
        "label": "Pipeline engine",
        "kind": "module",
        "responsibility": "Transforma o tipo de tarefa em configuração ordenada de etapas, políticas e templates.",
        "evidence": [
          {
            "path": "mcp-server/src/lib/pipeline-engine.ts",
            "quote": "export function buildPipeline(taskType: TaskType): PipelineConfig & { type: string } {"
          }
        ]
      },
      {
        "id": "task-classifier",
        "label": "TaskType classifier",
        "kind": "interface",
        "responsibility": "Fornece o tipo de tarefa usado como chave dos pipelines.",
        "evidence": [
          {
            "path": "mcp-server/src/lib/pipeline-engine.ts",
            "quote": "import type { TaskType } from \"./classifier.js\";"
          }
        ]
      },
      {
        "id": "file-reader",
        "label": "File reader",
        "kind": "module",
        "responsibility": "Lê arquivos e monta snippets de código a partir dos caminhos configurados.",
        "evidence": [
          {
            "path": "mcp-server/src/services/file-reader.ts",
            "quote": "export async function getCodeSnippets(type: string): Promise<Array<{ path: string; content: string }>> {"
          }
        ]
      },
      {
        "id": "filesystem",
        "label": "Filesystem",
        "kind": "external",
        "responsibility": "Fonte de conteúdo usada pelo leitor de arquivos.",
        "evidence": [
          {
            "path": "mcp-server/src/services/file-reader.ts",
            "quote": "    return await fs.readFile(filePath, \"utf-8\");"
          }
        ]
      },
      {
        "id": "playwright-adapter",
        "label": "Playwright adapter",
        "kind": "module",
        "responsibility": "Monta instruções de screenshot compatíveis com o cliente de browser.",
        "evidence": [
          {
            "path": "mcp-server/src/services/playwright.ts",
            "quote": "export function buildScreenshotInstruction(\n  url: string,\n  fullPage: boolean = true,"
          }
        ]
      },
      {
        "id": "playwright-mcp",
        "label": "Playwright MCP",
        "kind": "external",
        "responsibility": "Cliente externo que executa as instruções de browser descritas pelo serviço.",
        "evidence": [
          {
            "path": "mcp-server/src/services/playwright.ts",
            "quote": "// Playwright is used via the Playwright MCP that the client already has configured."
          }
        ]
      },
      {
        "id": "dedup-cache",
        "label": "Cross-call dedup cache",
        "kind": "module",
        "responsibility": "Mantém uma janela em memória de saídas recentes para detectar duplicação.",
        "evidence": [
          {
            "path": "mcp-server/src/lib/cross-call-dedup.ts",
            "quote": "export class CrossCallDedupCache {\n  private readonly windowSize: number;\n  private readonly threshold: number;"
          }
        ]
      },
      {
        "id": "similarity-engine",
        "label": "Hash e similaridade",
        "kind": "interface",
        "responsibility": "Usa hash exato e comparação de similaridade como caminhos de detecção.",
        "evidence": [
          {
            "path": "mcp-server/src/lib/cross-call-dedup.ts",
            "quote": "    const exactHash = fnv1a64(text);\n    // Fast path: exact hash match. Walk newest-to-oldest."
          }
        ]
      },
      {
        "id": "permission-guard",
        "label": "Permission ladder guard",
        "kind": "control",
        "responsibility": "Extrai comandos Bash recebidos pelo hook para aplicar a política de permissão.",
        "evidence": [
          {
            "path": "hooks/scripts/permission-ladder-guard.mjs",
            "quote": "function extractCommand(input) {\n  const name = input?.tool_name || \"\";\n  if (name === \"Bash\") return input?.tool_input?.command || \"\";"
          }
        ]
      },
      {
        "id": "bash-tool",
        "label": "Bash tool",
        "kind": "external",
        "responsibility": "Ferramenta cuja entrada é inspecionada pelo guard de permissões.",
        "evidence": [
          {
            "path": "hooks/scripts/permission-ladder-guard.mjs",
            "quote": "  if (name === \"Bash\") return input?.tool_input?.command || \"\";"
          }
        ]
      }
    ],
    "edges": [
      {
        "from": "auth-hook",
        "to": "auth-api",
        "relationship": "consome endpoint de login",
        "confidence": "observed",
        "evidence": [
          {
            "path": "src/hooks/useAuth.ts",
            "quote": "const { data } = await api.post<ApiResponse<AuthResponse>>('/auth/login', input);"
          }
        ]
      },
      {
        "from": "pagination-hook",
        "to": "paginated-query",
        "relationship": "delegates a consulta paginada",
        "confidence": "observed",
        "evidence": [
          {
            "path": "src/hooks/usePagination.ts",
            "quote": "  const query = usePaginatedQuery<T>(queryKey, url, allParams);"
          }
        ]
      },
      {
        "from": "pipeline-engine",
        "to": "task-classifier",
        "relationship": "importa o tipo da tarefa",
        "confidence": "observed",
        "evidence": [
          {
            "path": "mcp-server/src/lib/pipeline-engine.ts",
            "quote": "import type { TaskType } from \"./classifier.js\";"
          }
        ]
      },
      {
        "from": "file-reader",
        "to": "filesystem",
        "relationship": "lê conteúdo por fs.readFile",
        "confidence": "observed",
        "evidence": [
          {
            "path": "mcp-server/src/services/file-reader.ts",
            "quote": "    return await fs.readFile(filePath, \"utf-8\");"
          }
        ]
      },
      {
        "from": "playwright-adapter",
        "to": "playwright-mcp",
        "relationship": "prepara instruções para execução externa",
        "confidence": "inferred",
        "evidence": [
          {
            "path": "mcp-server/src/services/playwright.ts",
            "quote": "// This service provides helper functions for building Playwright-compatible instructions"
          }
        ]
      },
      {
        "from": "dedup-cache",
        "to": "similarity-engine",
        "relationship": "usa hash para detectar repetição",
        "confidence": "observed",
        "evidence": [
          {
            "path": "mcp-server/src/lib/cross-call-dedup.ts",
            "quote": "    const exactHash = fnv1a64(text);"
          }
        ]
      },
      {
        "from": "permission-guard",
        "to": "bash-tool",
        "relationship": "inspeciona a ferramenta Bash",
        "confidence": "observed",
        "evidence": [
          {
            "path": "hooks/scripts/permission-ladder-guard.mjs",
            "quote": "  if (name === \"Bash\") return input?.tool_input?.command || \"\";"
          }
        ]
      }
    ],
    "gaps": [
      "O mapa cobre somente os oito arquivos revisados; o wiring completo do servidor MCP e as dependências entre todos os módulos ainda precisam de leitura semântica.",
      "Não há evidência suficiente nesta amostra para desenhar organogramas de times, ownership ou deploy.",
      "A relação entre middleware e o hook de autenticação permanece fora do diagrama porque o trecho revisado não mostra uma chamada direta entre eles."
    ]
  },
  "workflows": {
    "summary": "Os fluxos revisados cobrem a inicialização do servidor MCP, o consumo de uma ferramenta pelo agente e a decisão do guard de comandos. São fluxos estáticos; não representam uma sessão de produção.",
    "items": [
      {
        "id": "mcp-startup",
        "title": "Inicialização do servidor MCP via stdio",
        "trigger": "O processo do servidor é iniciado pelo entrypoint compilado.",
        "actors": "Cliente MCP e processo Node do servidor",
        "steps": [
          "Instanciar StdioServerTransport.",
          "Conectar o servidor MCP ao transporte stdio.",
          "Manter a comunicação disponível para o cliente."
        ],
        "success": "O servidor conecta sem erro e registra a mensagem de execução.",
        "failure": "Falha de inicialização cai no catch de main e termina o processo com exit 1; não há prova de execução nesta análise.",
        "confidence": "observed",
        "evidence": [
          {
            "path": "mcp-server/src/index.ts",
            "quote": "  const transport = new StdioServerTransport();\n  await server.connect(transport);"
          }
        ]
      },
      {
        "id": "task-routing",
        "title": "Roteamento de uma tarefa para pipeline e plugins",
        "trigger": "O cliente chama a ferramenta devkit_route_task com uma descrição.",
        "actors": "Cliente MCP, classifier, pipeline engine e plugin router",
        "steps": [
          "Receber description e project_context opcionais.",
          "Classificar a tarefa.",
          "Construir o pipeline correspondente.",
          "Consultar a composição de plugins.",
          "Retornar a composição ao cliente."
        ],
        "success": "O resultado contém o tipo, as etapas do pipeline e a composição roteada.",
        "failure": "Falhas do roteador ou de dependências podem rejeitar a chamada; o contrato de erro completo não foi revisado nesta amostra.",
        "confidence": "observed",
        "evidence": [
          {
            "path": "mcp-server/src/index.ts",
            "quote": "server.registerTool(\n  \"devkit_route_task\","
          }
        ]
      },
      {
        "id": "permission-decision",
        "title": "Decomposição de comando para decisão do guard",
        "trigger": "O hook recebe um evento Bash com comando composto.",
        "actors": "Hook de permissão e comando Bash",
        "steps": [
          "Extrair o comando do payload do hook.",
          "Separar substituições e segmentos shell.",
          "Normalizar aspas simples em barewords.",
          "Comparar os segmentos com a escada de permissões.",
          "Permitir ou bloquear conforme a regra encontrada."
        ],
        "success": "Uma decisão estruturada é escrita no stdout quando o hook está habilitado.",
        "failure": "Payload inválido ou configuração desabilitada segue pelo caminho allow; a precedência de múltiplas regras permanece uma melhoria registrada.",
        "confidence": "observed",
        "evidence": [
          {
            "path": "hooks/scripts/permission-ladder-guard.mjs",
            "quote": "  const segments = [command];"
          }
        ]
      }
    ],
    "gaps": [
      "Não há evidência de uma chamada MCP real, de timeout/retry de ferramenta ou de execução de uma automação externa.",
      "Fluxos de autenticação do app consumidor não foram tratados como fluxo do servidor MCP."
    ]
  },
  "boundaries": {
    "summary": "Os boundaries observados se concentram no transporte MCP, no binário Node e na entrada de comandos do hook. Contratos HTTP externos exigem revisão adicional de cada service.",
    "items": [
      {
        "id": "mcp-stdio",
        "kind": "transport",
        "name": "Servidor MCP ↔ cliente",
        "interface": "StdioServerTransport conectado por server.connect(transport)",
        "auth": "Não documentada neste transporte; autorização de ferramentas é uma preocupação separada.",
        "input": "Mensagens MCP recebidas no stdio.",
        "output": "Respostas e resultados das ferramentas MCP no stdio.",
        "errors": "Falha em main registra erro e termina com exit 1.",
        "confidence": "observed",
        "evidence": [
          {
            "path": "mcp-server/src/index.ts",
            "quote": "  const transport = new StdioServerTransport();\n  await server.connect(transport);"
          }
        ]
      },
      {
        "id": "route-tool",
        "kind": "mcp-tool",
        "name": "devkit_route_task",
        "interface": "Ferramenta MCP registrada com description e project_context opcionais.",
        "auth": "Nenhum mecanismo de autenticação foi observado no registro desta ferramenta.",
        "input": "Descrição da tarefa e contexto opcional do projeto.",
        "output": "Tipo de tarefa, etapas do pipeline e composição de plugins.",
        "errors": "O contrato completo de erros do classifier/plugin-router não foi revisado nesta amostra.",
        "confidence": "observed",
        "evidence": [
          {
            "path": "mcp-server/src/index.ts",
            "quote": "server.registerTool(\n  \"devkit_route_task\","
          }
        ]
      },
      {
        "id": "package-entrypoint",
        "kind": "cli",
        "name": "dev-team-kit-mcp",
        "interface": "Binário npm que aponta para dist/index.js.",
        "auth": "Nenhuma autenticação no launcher; services podem exigir env vars.",
        "input": "Execução do binário e mensagens MCP no processo.",
        "output": "Servidor MCP compilado.",
        "errors": "Erros de compilação ou inicialização impedem o processo.",
        "confidence": "observed",
        "evidence": [
          {
            "path": "mcp-server/package.json",
            "quote": "    \"dev-team-kit-mcp\": \"dist/index.js\""
          }
        ]
      },
      {
        "id": "environment-loader",
        "kind": "configuration",
        "name": "Arquivos de ambiente",
        "interface": "loadEnvFile lê .env.local e .env no projeto e no KIT_ROOT.",
        "auth": "Valores são credenciais potenciais e não devem ser publicados.",
        "input": "Arquivos de ambiente e process.env.",
        "output": "Variáveis disponíveis para services.",
        "errors": "Falhas de leitura são capturadas e seguem para o próximo arquivo/base.",
        "confidence": "observed",
        "evidence": [
          {
            "path": "mcp-server/src/index.ts",
            "quote": "  for (const envFile of [\".env.local\", \".env\"]) {"
          }
        ]
      }
    ],
    "gaps": [
      "Os endpoints Brave, Firecrawl e FAL aparecem no código, mas seus services não foram incluídos na revisão semântica deste snapshot.",
      "Não há exemplos de request/response MCP completos nem matriz de erros por ferramenta."
    ]
  },
  "database": {
    "status": "partial",
    "summary": "O template stack-default contém um schema Drizzle/Postgres separado do runtime principal do servidor MCP. O snapshot documenta as tabelas Better Auth visíveis no schema, sem afirmar migrações ou banco implantado.",
    "entities": [
      {
        "name": "user",
        "kind": "Drizzle pgTable",
        "fields": "id, name, email, emailVerified, image, createdAt, updatedAt",
        "relations": "É referenciada por session e account via userId.",
        "evidence": [{"path":"templates/stack-default/apps/web/src/db/schema.ts","quote":"export const user = pgTable(\"user\", {"}]
      },
      {
        "name": "session",
        "kind": "Drizzle pgTable",
        "fields": "id, expiresAt, token, createdAt, updatedAt, ipAddress, userAgent, userId",
        "relations": "userId referencia user.id com onDelete cascade.",
        "evidence": [{"path":"templates/stack-default/apps/web/src/db/schema.ts","quote":"export const session = pgTable(\"session\", {"}]
      },
      {
        "name": "account",
        "kind": "Drizzle pgTable",
        "fields": "id, accountId, providerId, userId, accessToken, refreshToken, idToken, scope, password, timestamps",
        "relations": "userId referencia user.id com onDelete cascade.",
        "evidence": [{"path":"templates/stack-default/apps/web/src/db/schema.ts","quote":"export const account = pgTable(\"account\", {"}]
      },
      {
        "name": "verification",
        "kind": "Drizzle pgTable",
        "fields": "id, identifier, value, expiresAt, timestamps",
        "relations": "Nenhuma relação explícita foi observada neste schema.",
        "evidence": [{"path":"templates/stack-default/apps/web/src/db/schema.ts","quote":"export const verification = pgTable(\"verification\", {"}]
      }
    ],
    "gaps": [
      "O template não foi tratado como banco do servidor MCP principal.",
      "Migrações, índices adicionais, queries de domínio e banco de produção não foram executados ou revisados."
    ]
  },
  "modules": {
    "summary": "Deep dives dos módulos centrais revisados, selecionados por papel no entrypoint, fan-in aparente e impacto de segurança/execução.",
    "items": [
      {
        "id": "mcp-entrypoint",
        "name": "MCP server entrypoint",
        "path": "mcp-server/src/index.ts",
        "purpose": "Inicializa configuração, registra ferramentas MCP e conecta o transporte stdio.",
        "inputs": "Payloads MCP, project context e variáveis de ambiente.",
        "outputs": "Resultados de ferramentas e processo MCP conectado.",
        "dependencies": "McpServer, StdioServerTransport, Zod, services e libs internas.",
        "tests": "Build e testes compilados do pacote foram executados localmente; cobertura por ferramenta não foi medida.",
        "risks": "Arquivo grande concentra registro e composição de muitas ferramentas; falhas de inicialização afetam todo o servidor.",
        "evidence": [{"path":"mcp-server/src/index.ts","quote":"const server = new McpServer({"}]
      },
      {
        "id": "pipeline-engine",
        "name": "Pipeline engine",
        "path": "mcp-server/src/lib/pipeline-engine.ts",
        "purpose": "Transforma o tipo de tarefa em configuração ordenada de etapas.",
        "inputs": "TaskType e índice da etapa corrente.",
        "outputs": "PipelineConfig e próxima etapa ou null.",
        "dependencies": "Tipo TaskType definido pelo classifier.",
        "tests": "A suíte do pacote foi executada, mas não há teste dedicado de todos os índices nesta revisão.",
        "risks": "Índices negativos, fracionários e NaN ainda são uma melhoria proposta.",
        "evidence": [{"path":"mcp-server/src/lib/pipeline-engine.ts","quote":"export function buildPipeline(taskType: TaskType): PipelineConfig & { type: string } {"}]
      },
      {
        "id": "file-reader",
        "name": "File reader",
        "path": "mcp-server/src/services/file-reader.ts",
        "purpose": "Lê arquivos permitidos e monta snippets para as ferramentas do kit.",
        "inputs": "Caminhos e tipos de snippet.",
        "outputs": "Texto de arquivo ou lista de snippets; erro de leitura vira null no helper observado.",
        "dependencies": "Filesystem do processo Node.",
        "tests": "Não foi encontrado teste dedicado do caminho de erro de filesystem nesta amostra.",
        "risks": "Catch amplo pode esconder permissão/I/O como ausência; há melhoria MEL-03 para separar os casos.",
        "evidence": [{"path":"mcp-server/src/services/file-reader.ts","quote":"export async function readFile(filePath: string): Promise<string | null> {"}]
      },
      {
        "id": "permission-guard",
        "name": "Permission ladder guard",
        "path": "hooks/scripts/permission-ladder-guard.mjs",
        "purpose": "Classifica comandos Bash em lanes de permissão e retorna decisão ao hook.",
        "inputs": "Payload JSON do evento de ferramenta.",
        "outputs": "JSON de continuidade ou bloqueio no stdout.",
        "dependencies": "Configuração do hook, regex da ladder e parsing de segmentos shell.",
        "tests": "A revisão leu a decomposição e a suíte geral do repositório foi validada; combinações de precedência continuam pendentes.",
        "risks": "Regex não substitui parser shell completo; a precedência de regras em comandos compostos exige cobertura adversarial.",
        "evidence": [{"path":"hooks/scripts/permission-ladder-guard.mjs","quote":"  const segments = [command];"}]
      }
    ],
    "gaps": [
      "O deep dive ainda não cobre todos os módulos com alto fan-in do pacote MCP.",
      "Owners não foram preenchidos porque não há evidência de ownership no recorte revisado."
    ]
  },
  "runtime_verification": {
    "status": "pass",
    "commands": [
      {
        "command": "npm run build",
        "purpose": "Compilar o servidor MCP TypeScript.",
        "status": "pass",
        "exit_code": 0,
        "notes": "Executado localmente em mcp-server com Node v22.23.2."
      },
      {
        "command": "npm test",
        "purpose": "Executar testes compilados de deduplicação, manifesto e roteador de plugins.",
        "status": "pass",
        "exit_code": 0,
        "notes": "Executado localmente; 11 + 4 + 25 casos reportados sem falha."
      }
    ],
    "gaps": [
      "O servidor MCP não foi iniciado contra um cliente real nem chamado por uma sessão MCP.",
      "Nenhuma integração externa, deploy ou automação de browser foi executada."
    ]
  },
  "findings": [
    {
      "id": "RN-01",
      "track": "business_rules",
      "kind": "operational",
      "title": "O tipo de tarefa determina a sequência de trabalho",
      "domain": "Orquestração",
      "condition": "Um consumidor chama buildPipeline com bugfix ou hotfix.",
      "effect": "Bugfix recebe auditoria do repositório, QA, segurança e revisão final. Hotfix recebe segurança, revisão e deploy. A função devolve a configuração ordenada.",
      "exceptions": "O retorno é um plano; esta função não executa testes nem deploy e não prova que etapas foram cumpridas.",
      "verification": "Leitura estática do código citado. O fluxo não foi executado em uma aplicação consumidora neste levantamento.",
      "confidence": "observed",
      "evidence": [
        {
          "path": "mcp-server/src/lib/pipeline-engine.ts",
          "quote": "  bugfix: {\n    steps: [\n      { id: \"18-repo-auditor\", name: \"Repo Auditor\", purpose: \"entender contexto do bug\" },\n      { id: \"05-qa-testing\", name: \"QA\", purpose: \"reproduzir e testar fix\" },\n      { id: \"06-security-review\", name: \"Security\", purpose: \"validar se fix nao abre brecha\" },\n      { id: \"11-reviewer\", name: \"Reviewer\", purpose: \"validacao final\" },\n    ],\n    policies: [\"execution\", \"handoffs\", \"quality-gates\"],\n    templates: [\"handoff\", \"review\"],\n  },\n  ui_improvement:"
        },
        {
          "path": "mcp-server/src/lib/pipeline-engine.ts",
          "quote": "  hotfix: {\n    steps: [\n      { id: \"06-security-review\", name: \"Security\", purpose: \"validar fix critico\" },\n      { id: \"11-reviewer\", name: \"Reviewer\", purpose: \"validacao rapida\" },\n      { id: \"07-deploy\", name: \"Deploy\", purpose: \"deploy urgente\" },\n    ],\n    policies: [\"execution\", \"quality-gates\"],\n    templates: [\"handoff\"],\n  },\n  release:"
        },
        {
          "path": "mcp-server/src/lib/pipeline-engine.ts",
          "quote": "export function buildPipeline(taskType: TaskType): PipelineConfig & { type: string } {\n  const config = PIPELINES[taskType];\n  return {\n    type: taskType,\n    ...config,\n  };\n}\n\nexport function getNextStep"
        }
      ]
    },
    {
      "id": "RN-02",
      "track": "business_rules",
      "kind": "operational",
      "title": "Saídas repetidas podem ser substituídas por referência à chamada anterior",
      "domain": "Economia de contexto",
      "condition": "check recebe texto com hash igual a uma chamada na janela; na ausência de igualdade, avalia similaridade das assinaturas.",
      "effect": "Uma igualdade retorna marcador com ID da chamada mais recente correspondente; similaridade igual ou superior ao limiar retorna marcador percentual da melhor correspondência. Sem correspondência, replacement é null.",
      "exceptions": "O padrão é uma janela de 16 e limiar de 0,85. check não insere. A comparação aproximada não prova equivalência semântica; o chamador decide aplicar a substituição.",
      "verification": "Leitura estática do código citado. O fluxo não foi executado em uma aplicação consumidora neste levantamento.",
      "confidence": "observed",
      "evidence": [
        {
          "path": "mcp-server/src/lib/cross-call-dedup.ts",
          "quote": "  constructor(opts: CrossCallDedupOptions = {}) {\n    this.windowSize = Math.max(1, opts.windowSize ?? 16);\n    this.threshold = Math.min(1, Math.max(0, opts.threshold ?? 0.85));\n  }\n\n  /**\n   * Check the cache. Does NOT insert. Returns a replacement string when the\n   * incoming text matches a recent call exactly or fuzzily.\n   */\n  check(text: string): DedupResult {\n    if (this.records.length === 0) return { replacement: null };\n\n    const exactHash = fnv1a64(text);\n    // Fast path: exact hash match. Walk newest-to-oldest.\n    for (let i = this.records.length - 1; i >= 0; i--) {\n      const r = this.records[i];\n      if (r.exactHash === exactHash) {\n        return {\n          replacement: `[squeez-style: identical to call #${r.callId}${r.label ? ` (${r.label})` : \"\"}]`,\n          match: { callId: r.callId, kind: \"exact\", similarity: 1, label: r.label },\n        };\n      }\n    }\n\n    // Fuzzy path: compute signature, scan window, pick best above threshold.\n    const sig = shingleMinHash(text);\n    let best: { record: CallRecord; sim: number } | null = null;\n    for (let i = this.records.length - 1; i >= 0; i--) {\n      const r = this.records[i];\n      const sim = jaccard(sig, r.signature);\n      if (sim >= this.threshold && (best === null || sim > best.sim)) {\n        best = { record: r, sim };\n      }\n    }\n\n    if (best !== null) {\n      const pct = Math.round(best.sim * 100);\n      return {\n        replacement: `[squeez-style: ~${pct}% similar to call #${best.record.callId}${best.record.label ? ` (${best.record.label})` : \"\"}]`,\n        match: {\n          callId: best.record.callId,\n          kind: \"fuzzy\",\n          similarity: best.sim,\n          label: best.record.label,\n        },\n      };\n    }\n\n    return { replacement: null };\n  }\n\n  /**\n   * Insert a new call"
        }
      ]
    },
    {
      "id": "RN-03",
      "track": "business_rules",
      "kind": "operational",
      "title": "O cache descarta a chamada mais antiga quando excede a janela",
      "domain": "Retenção do histórico",
      "condition": "insert adiciona uma chamada e o número de registros ultrapassa windowSize.",
      "effect": "O registro mais antigo é removido. checkAndInsert insere mesmo quando encontrou repetição. clear esvazia a janela e reinicia os IDs em 1.",
      "exceptions": "A classe mantém estado em memória, não um histórico persistente. Reiniciar a sessão exige que o consumidor limpe ou substitua a instância.",
      "verification": "Leitura estática do código citado. O fluxo não foi executado em uma aplicação consumidora neste levantamento.",
      "confidence": "observed",
      "evidence": [
        {
          "path": "mcp-server/src/lib/cross-call-dedup.ts",
          "quote": "  insert(text: string, label?: string): CallRecord {\n    const record: CallRecord = {\n      callId: this.nextId++,\n      exactHash: fnv1a64(text),\n      signature: shingleMinHash(text),\n      label,\n    };\n    this.records.push(record);\n    if (this.records.length > this.windowSize) {\n      this.records.shift();\n    }\n    return record;\n  }\n\n  /**\n   * Convenience: check first, then insert (always). Returns the dedup result.\n   * Inserting after a match lets future calls match against the same anchor.\n   */\n  checkAndInsert(text: string, label?: string): DedupResult {\n    const result = this.check(text);\n    this.insert(text, label);\n    return result;\n  }\n\n  /** Current number of records in the window. */\n  size(): number {\n    return this.records.length;\n  }\n\n  /** Reset the window. Useful between sessions or for tests. */\n  clear(): void {\n    this.records = [];\n    this.nextId = 1;\n  }\n}\n\n// ─── Default singleton"
        }
      ]
    },
    {
      "id": "RN-04",
      "track": "business_rules",
      "kind": "operational",
      "title": "Tipos desconhecidos não retornam snippets",
      "domain": "Distribuição de exemplos",
      "condition": "getCodeSnippets recebe uma categoria de código.",
      "effect": "Categorias reconhecidas selecionam hooks, components, stores, types ou middleware. Uma categoria desconhecida retorna lista vazia; middleware retorna apenas o exemplo correspondente quando legível.",
      "exceptions": "Isso identifica src como biblioteca de exemplos exposta pelo serviço, não prova que uma aplicação Next.js esteja implantada. Falhas de leitura são convertidas em null pelo helper.",
      "verification": "Leitura estática do código citado. O fluxo não foi executado em uma aplicação consumidora neste levantamento.",
      "confidence": "observed",
      "evidence": [
        {
          "path": "mcp-server/src/services/file-reader.ts",
          "quote": "export async function getCodeSnippets(type: string): Promise<Array<{ path: string; content: string }>> {\n  const typeMap: Record<string, string> = {\n    hooks: \"hooks\",\n    components: \"components\",\n    stores: \"stores\",\n    types: \"types\",\n    middleware: \"\",\n  };\n\n  const subDir = typeMap[type];\n  if (subDir === undefined) return [];\n\n  if (type === \"middleware\") {\n    const content = await readFile(path.join(PATHS.src, \"middleware.ts\"));\n    return content ? [{ path: \"src/middleware.ts\", content }] : [];\n  }\n\n  const files = await glob(\"**/*.{ts,tsx}\", { cwd: path.join(PATHS.src, subDir) });\n  const results: Array<{ path: string; content: string }> = [];\n\n  for (const file of files) {\n    const content = await readFile(path.join(PATHS.src, subDir, file));\n    if (content) {\n      results.push({ path: `src/${subDir}/${file}`, content });\n    }\n  }\n\n  return results;\n}\n\nexport async function getRepoAudit"
        },
        {
          "path": "mcp-server/src/services/file-reader.ts",
          "quote": "export async function readFile(filePath: string): Promise<string | null> {\n  try {\n    return await fs.readFile(filePath, \"utf-8\");\n  } catch {\n    return null;\n  }\n}\n\nexport async function listSkills"
        }
      ]
    },
    {
      "id": "RN-05",
      "track": "business_rules",
      "kind": "reference",
      "title": "Logout encerra a sessão local mesmo quando a API falha",
      "domain": "Sessão do exemplo de aplicação",
      "condition": "A mutação de logout termina, com sucesso ou erro.",
      "effect": "onSettled limpa o estado de autenticação e o cache de consultas e navega para /login.",
      "exceptions": "Não comprova revogação do token no servidor em caso de erro. É comportamento do snippet de referência, não uma regra de produto em produção.",
      "verification": "Leitura estática do código citado. O fluxo não foi executado em uma aplicação consumidora neste levantamento.",
      "confidence": "observed",
      "evidence": [
        {
          "path": "src/hooks/useAuth.ts",
          "quote": "  const logoutMutation = useMutation({\n    mutationFn: async () => {\n      await api.post('/auth/logout');\n    },\n    onSettled: () => {\n      clearAuth();\n      queryClient.clear();\n      router.push('/login');\n    },\n  });\n\n  // Me query"
        }
      ]
    },
    {
      "id": "RN-06",
      "track": "business_rules",
      "kind": "reference",
      "title": "Navegação de páginas respeita os limites conhecidos",
      "domain": "Listagem do exemplo de aplicação",
      "condition": "goToPage, nextPage ou prevPage são chamados com metadados de paginação disponíveis.",
      "effect": "goToPage limita o destino entre 1 e totalPages; nextPage usa o menor valor entre próxima página e totalPages; prevPage não desce de 1. Sem metadados, totalPages vale 1.",
      "exceptions": "initialPage não é normalizado e não há validação de NaN ou números fracionários. A regra descreve entradas numéricas válidas e não garante correção para metadados inválidos.",
      "verification": "Leitura estática do código citado. O fluxo não foi executado em uma aplicação consumidora neste levantamento.",
      "confidence": "observed",
      "evidence": [
        {
          "path": "src/hooks/usePagination.ts",
          "quote": "  const meta = query.data?.meta;\n  const totalPages = meta?.totalPages ?? 1;\n\n  const goToPage = useCallback((p: number) => {\n    setPage(Math.max(1, Math.min(p, totalPages)));\n  }, [totalPages]);\n\n  const nextPage = useCallback(() => {\n    setPage((p) => Math.min(p + 1, totalPages));\n  }, [totalPages]);\n\n  const prevPage = useCallback(() => {\n    setPage((p) => Math.max(p - 1, 1));\n  }, []);\n\n  // Gera array"
        }
      ]
    },
    {
      "id": "SEC-01",
      "track": "security",
      "kind": "reference",
      "title": "O middleware verifica presença de cookie, não validade da sessão",
      "domain": "Autenticação",
      "condition": "Uma rota não dispensada passa pela checagem do middleware.",
      "effect": "A presença do cookie refresh-token é usada como hasSession. Sem cookie, a rota protegida redireciona para login; nas rotas de login/cadastro, a presença do cookie redireciona para dashboard.",
      "exceptions": "Não há validação JWT neste arquivo. Prefixos de APIs públicas e caminhos contendo ponto retornam antes da checagem. A proteção efetiva do backend não foi revisada; isto não é certificação de acesso seguro.",
      "verification": "Leitura estática do código citado. O fluxo não foi executado em uma aplicação consumidora neste levantamento.",
      "confidence": "observed",
      "evidence": [
        {
          "path": "src/middleware.ts",
          "quote": "export function middleware(request: NextRequest) {\n  const { pathname } = request.nextUrl;\n\n  // Ignora assets estáticos e API routes públicas\n  if (\n    pathname.startsWith('/_next') ||\n    pathname.startsWith('/favicon') ||\n    pathname.includes('.') ||\n    publicApiRoutes.some((r) => pathname.startsWith(r))\n  ) {\n    return NextResponse.next();\n  }\n\n  // Checa se tem token (via cookie de session ou header)\n  // Em produção, validar JWT server-side aqui\n  const hasSession = request.cookies.has('refresh-token');\n\n  // Rota pública: permite acesso\n  if (publicRoutes.some((r) => pathname.startsWith(r))) {\n    // Se já autenticado, redireciona pra dashboard\n    if (hasSession && authRoutes.some((r) => pathname.startsWith(r))) {\n      return NextResponse.redirect(new URL('/dashboard', request.url));\n    }\n    return NextResponse.next();\n  }\n\n  // Rota protegida sem sessão: redireciona pra login\n  if (!hasSession) {\n    const loginUrl = new URL('/login', request.url);\n    loginUrl.searchParams.set('redirect', pathname);\n    return NextResponse.redirect(loginUrl);\n  }\n\n  // Security headers"
        }
      ]
    },
    {
      "id": "SEC-02",
      "track": "security",
      "kind": "operational",
      "title": "O guard diferencia pedir aprovação de negar a ação",
      "domain": "Permissão de ferramentas",
      "condition": "Com o guard habilitado, um comando Bash casa com uma regra.",
      "effect": "A primeira correspondência decide: lane closed produz deny; as demais produzem ask, salvo escape explícito permitido apenas para essas demais lanes.",
      "exceptions": "O padrão é desabilitado. Entrada JSON inválida, ferramenta diferente de Bash ou ausência de match libera. Regex não interpreta toda a linguagem de shell; combinações podem selecionar uma regra menos restritiva antes da closed.",
      "verification": "Leitura estática do código citado. O fluxo não foi executado em uma aplicação consumidora neste levantamento.",
      "confidence": "observed",
      "evidence": [
        {
          "path": "hooks/scripts/permission-ladder-guard.mjs",
          "quote": "function extractCommand(input) {\n  const name = input?.tool_name || \"\";\n  if (name === \"Bash\") return input?.tool_input?.command || \"\";\n  return \"\";\n}\n\n// Splits"
        },
        {
          "path": "hooks/scripts/permission-ladder-guard.mjs",
          "quote": "  if (isHookDisabled(\"permission-ladder-guard\")) return allow();\n\n  let input = {};\n  try { input = JSON.parse(inputBuffer); } catch { return allow(); }\n\n  const cfg = readHookConfig(\"permission_ladder_guard\", { enabled: false });\n  if (cfg.enabled !== true) return allow();\n\n  const command = extractCommand(input);\n  if (!command) return allow();\n\n  const segments = splitCompoundCommand(command).map(stripCommandQuoting);\n  let hit = null;\n  let matchedSegment = command;\n  for (const segment of segments) {\n    hit = LADDER.find((rule) => rule.pattern.test(segment));\n    if (hit) { matchedSegment = segment; break; }\n  }\n  if (!hit) return allow();\n\n  const isClosedLane = hit.lane === \"closed\";\n\n  // Escape hatch, same convention as design-anchor-guard and dev-guard — but\n  // it does not apply to a closed lane. A lane that any suffix can open is a\n  // threshold, not a closed lane, and thresholds are exactly what this is\n  // meant not to be.\n  if (!isClosedLane && /permission-ladder:\\s*allow/i.test(command)) return allow();\n\n  const reason ="
        },
        {
          "path": "hooks/scripts/permission-ladder-guard.mjs",
          "quote": "  process.stdout.write(JSON.stringify({\n    hookSpecificOutput: {\n      hookEventName: \"PreToolUse\",\n      // Closed lane denies outright: there is no approval that opens it from\n      // inside the agent loop. Everything else asks a human.\n      permissionDecision: isClosedLane ? \"deny\" : \"ask\",\n      permissionDecisionReason: reason,\n    },\n  }));"
        }
      ]
    },
    {
      "id": "AUTO-01",
      "track": "automations",
      "kind": "operational",
      "title": "A consulta da próxima etapa usa índice de base zero",
      "domain": "Pipeline",
      "condition": "getNextStep recebe tipo e posição atual.",
      "effect": "Retorna a etapa na posição informada; tipo inexistente ou posição a partir do fim retorna null.",
      "exceptions": "Não incrementa a posição, não executa a etapa e não verifica sua conclusão. Índices negativos ou fracionários não são rejeitados explicitamente e podem retornar undefined.",
      "verification": "Leitura estática do código citado. O fluxo não foi executado em uma aplicação consumidora neste levantamento.",
      "confidence": "observed",
      "evidence": [
        {
          "path": "mcp-server/src/lib/pipeline-engine.ts",
          "quote": "export function getNextStep(\n  pipelineType: TaskType,\n  currentStep: number,\n): { id: string; name: string; purpose: string } | null {\n  const config = PIPELINES[pipelineType];\n  if (!config) return null;\n\n  const nextIndex = currentStep;\n  if (nextIndex >= config.steps.length) return null;\n\n  return config.steps[nextIndex];\n}"
        }
      ]
    },
    {
      "id": "RPA-01",
      "track": "rpa",
      "kind": "operational",
      "title": "O serviço de browser prepara instruções, não executa o processo",
      "domain": "Integração com automação de navegador",
      "condition": "O cliente solicita instrução de screenshot, extração de imagens ou scraping.",
      "effect": "O serviço retorna URL e parâmetros descritivos. Screenshot assume página inteira; imagens usam seletor img e limite configurado. A execução depende de um consumidor externo.",
      "exceptions": "Não há lançamento de browser, login, retry, idempotência ou fila de exceções nestas funções. Nenhum processo de RPA operacional foi comprovado neste recorte.",
      "verification": "Leitura estática do código citado. O fluxo não foi executado em uma aplicação consumidora neste levantamento.",
      "confidence": "observed",
      "evidence": [
        {
          "path": "mcp-server/src/services/playwright.ts",
          "quote": "export function buildScreenshotInstruction(\n  url: string,\n  fullPage: boolean = true,\n  viewport?: { width: number; height: number },\n): ScreenshotInstruction {\n  return {\n    url,\n    fullPage,\n    viewport: viewport || DEFAULTS.screenshotViewport,\n    description: `Navigate to ${url} and take a ${fullPage ? \"full-page\" : \"viewport\"} screenshot`,\n  };\n}\n\nexport interface ImageExtractionInstruction {\n  url: string;\n  selector: string;\n  limit: number;\n  description: string;\n}\n\nexport function buildImageExtractionInstruction(\n  url: string,\n  selector: string = \"img\",\n  limit: number = DEFAULTS.maxImages,\n): ImageExtractionInstruction {\n  return {\n    url,\n    selector,\n    limit,\n    description: `Navigate to ${url}, query all '${selector}' elements, extract src and alt attributes (limit ${limit})`,\n  };\n}\n\nexport function buildScrapeInstruction(url: string): {\n  url: string;\n  description: string;\n} {\n  return {\n    url,\n    description: `Navigate to ${url}, extract page title and main text content as markdown`,\n  };\n}"
        }
      ]
    },
    {
      "id": "MEL-01",
      "track": "improvements",
      "kind": "reference",
      "title": "Validar sessão e revisar as exceções de rotas",
      "domain": "Segurança e produto",
      "condition": "Um app consumidor usa o middleware de exemplo como barreira de acesso.",
      "effect": "Proposta P1: validar token no servidor e substituir os atalhos de ponto/prefixo por regras de rota explícitas. Aplicar headers também às respostas antecipadas. Impacto: reduzir a diferença entre o redirecionamento da UI e autorização real.",
      "exceptions": "Não implementado nesta revisão. Depende do contrato de sessão do backend; esforço médio, risco de bloquear rotas públicas legítimas. Os outros endpoints não foram auditados.",
      "verification": "Proposto: testar cookie inválido/expirado, caminho com ponto, prefixo semelhante a API pública, rotas realmente públicas e headers em todos os retornos.",
      "confidence": "inferred",
      "evidence": [
        {
          "path": "src/middleware.ts",
          "quote": "export function middleware(request: NextRequest) {\n  const { pathname } = request.nextUrl;\n\n  // Ignora assets estáticos e API routes públicas\n  if (\n    pathname.startsWith('/_next') ||\n    pathname.startsWith('/favicon') ||\n    pathname.includes('.') ||\n    publicApiRoutes.some((r) => pathname.startsWith(r))\n  ) {\n    return NextResponse.next();\n  }\n\n  // Checa se tem token (via cookie de session ou header)\n  // Em produção, validar JWT server-side aqui\n  const hasSession = request.cookies.has('refresh-token');\n\n  // Rota pública: permite acesso\n  if (publicRoutes.some((r) => pathname.startsWith(r))) {\n    // Se já autenticado, redireciona pra dashboard\n    if (hasSession && authRoutes.some((r) => pathname.startsWith(r))) {\n      return NextResponse.redirect(new URL('/dashboard', request.url));\n    }\n    return NextResponse.next();\n  }\n\n  // Rota protegida sem sessão: redireciona pra login\n  if (!hasSession) {\n    const loginUrl = new URL('/login', request.url);\n    loginUrl.searchParams.set('redirect', pathname);\n    return NextResponse.redirect(loginUrl);\n  }\n\n  // Security headers em todas as respostas\n  const response = NextResponse.next();\n  response.headers.set('X-Frame-Options', 'DENY');\n  response.headers.set('X-Content-Type-Options', 'nosniff');\n  response.headers.set('Referrer-Policy', 'strict-origin-when-cross-origin');\n\n  return response;\n}"
        }
      ]
    },
    {
      "id": "MEL-02",
      "track": "improvements",
      "kind": "operational",
      "title": "Priorizar a restrição mais forte em comandos compostos",
      "domain": "Segurança das automações",
      "condition": "Um comando contém mais de uma operação que casa com regras distintas.",
      "effect": "Proposta P1: avaliar todas as correspondências antes de decidir e fazer closed prevalecer sobre gated. Impacto: a ordem dos segmentos não deve reduzir a restrição.",
      "exceptions": "Não implementado. Esforço médio, risco de falsos positivos. A melhoria não torna regex um parser completo de shell.",
      "verification": "Proposto: fixtures gated→closed, closed→gated, substituição de comando e escape; todas as combinações contendo closed devem negar.",
      "confidence": "inferred",
      "evidence": [
        {
          "path": "hooks/scripts/permission-ladder-guard.mjs",
          "quote": "  const segments = [command];\n  for (const m of command.matchAll(/\\$\\(([^)]*)\\)|`([^`]*)`/g)) {\n    segments.push(m[1] ?? m[2] ?? \"\");\n  }\n  const flat = segments.flatMap((s) => s.split(/&&|\\|\\||[;|\\n]/));\n  return flat.map((s) => s.trim()).filter(Boolean);\n}\n\n// A shell treats `\"rm\"` and `rm` identically; the pattern's word-boundary\n// regex does not, because a straddling quote character breaks \\b. Strip\n// paired quotes immediately around a bareword before matching so this one\n// specific dodge (there's no way to close all of them with regex, see the\n// header note) doesn't slip through for free.\nfunction stripCommandQuoting(command) {\n  return command.replace(/(^|[\\s;&|])[\"']([\\w./-]+)[\"']/g, \"$1$2\");\n}\n\nlet inputBuffer = \"\";\nprocess.stdin.setEncoding(\"utf-8\");\nprocess.stdin.on(\"data\", (chunk) => { inputBuffer += chunk; });\n\nprocess.stdin.on(\"end\", () => {\n  const allow = () => {\n    process.stdout.write(JSON.stringify({ continue: true }));\n    process.exit(0);\n  };\n\n  if (isHookDisabled(\"permission-ladder-guard\")) return allow();\n\n  let input = {};\n  try { input = JSON.parse(inputBuffer); } catch { return allow(); }\n\n  const cfg = readHookConfig(\"permission_ladder_guard\", { enabled: false });\n  if (cfg.enabled !== true) return allow();\n\n  const command = extractCommand(input);\n  if (!command) return allow();\n\n  const segments = splitCompoundCommand(command).map(stripCommandQuoting);\n  let hit = null;\n  let matchedSegment = command;\n  for (const segment of segments) {\n    hit = LADDER.find((rule) => rule.pattern.test(segment));\n    if (hit) { matchedSegment = segment; break; }\n  }\n  if (!hit) return allow();\n\n  const isClosedLane = hit.lane === \"closed\";\n\n  // Escape hatch, same convention as design-anchor-guard and dev-guard — but\n  // it does not apply to a closed lane. A lane that any suffix can open is a\n  // threshold, not a closed lane, and thresholds are exactly what this is\n  // meant not to be.\n  if (!isClosedLane && /permission-ladder:\\s*allow/i.test(command)) return allow();\n\n  const reason ="
        }
      ]
    },
    {
      "id": "MEL-03",
      "track": "improvements",
      "kind": "operational",
      "title": "Distinguir arquivo ausente de falha de leitura",
      "domain": "Confiabilidade e diagnóstico",
      "condition": "readFile recebe erro de permissão, I/O ou arquivo inexistente.",
      "effect": "Proposta P2: devolver ausência apenas para ENOENT e reportar as demais falhas com contexto sanitizado. Impacto: o catálogo não deve parecer vazio quando a leitura falhou.",
      "exceptions": "Não implementado. Esforço pequeno; mudar de null para erro pode exigir ajuste dos consumidores e compatibilidade do contrato.",
      "verification": "Proposto: testar arquivo ausente, permissão negada, leitura válida e propagação controlada do erro no serviço.",
      "confidence": "inferred",
      "evidence": [
        {
          "path": "mcp-server/src/services/file-reader.ts",
          "quote": "export async function readFile(filePath: string): Promise<string | null> {\n  try {\n    return await fs.readFile(filePath, \"utf-8\");\n  } catch {\n    return null;\n  }\n}\n\nexport async function listSkills"
        }
      ]
    },
    {
      "id": "MEL-04",
      "track": "improvements",
      "kind": "operational",
      "title": "Validar a posição antes de consultar a etapa",
      "domain": "Contratos, testes e DX",
      "condition": "Um consumidor envia índice negativo, fracionário ou NaN a getNextStep.",
      "effect": "Proposta P2: exigir inteiro não negativo e escolher contrato explícito de erro/null. Impacto: impedir retorno undefined quando o tipo anunciado promete etapa ou null.",
      "exceptions": "Não implementado. Esforço pequeno; é preciso decidir e documentar compatibilidade de erro para clientes existentes.",
      "verification": "Proposto: testar -1, 0, último índice, tamanho do pipeline, fração e NaN.",
      "confidence": "inferred",
      "evidence": [
        {
          "path": "mcp-server/src/lib/pipeline-engine.ts",
          "quote": "export function getNextStep(\n  pipelineType: TaskType,\n  currentStep: number,\n): { id: string; name: string; purpose: string } | null {\n  const config = PIPELINES[pipelineType];\n  if (!config) return null;\n\n  const nextIndex = currentStep;\n  if (nextIndex >= config.steps.length) return null;\n\n  return config.steps[nextIndex];\n}"
        }
      ]
    }
  ],
  "gaps": [
      "Cobertura parcial: apenas os treze arquivos listados foram revisados semanticamente. O inventário do restante não equivale a leitura humana/por agente.",
    "Nenhum fluxo de autenticação, deploy ou RPA foi executado em produção.",
    "Produto, UX/acessibilidade, infraestrutura/deploy, banco de dados e observabilidade fora dos trechos citados continuam pendentes; não foram preenchidos com recomendações genéricas.",
    "Propostas de melhoria são inferências rastreáveis, não alterações autorizadas nos módulos auditados."
  ]
}
